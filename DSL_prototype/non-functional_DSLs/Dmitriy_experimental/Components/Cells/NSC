define immovable cell NeuralStemCell from StemCell
    molecules:
        C: 0.45
        X: 1.1

    compartments:
        none

    conditions:
        none

    mechanisms:
        declaration:
            cyclic NSCToNPCAsyncDivide(duration: 1000) from Divide
                parameters: (_, NeuralProgenitorCell)

            cyclic NSCFSpread(duration: 200) from SignalSpread
                parameters: (NSCF)

            cyclic CyclinDynamic(duration: 200) from Dynamic
                parameters: (C)
                delta: C / 35 + (C - X) / X

        initial:
            NSCToNPCAsyncDivide
            NSCFSpread
            CyclinDynamic

        mutually exclusive:
            none