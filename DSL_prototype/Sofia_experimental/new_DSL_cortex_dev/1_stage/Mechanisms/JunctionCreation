
define JunctionCreation::Contacts
    (Cell) => (J)       #need to specify creation of contacts with
    duration = 10       #those neighbors where contacts are currently absent
                        #such as:

                        #neighbors = {a: 0, b: 0, c: 0, d: 0, e: 0, f: m}
                        #0 means nothing, 1 means cell, J means junction, m means BM
                        #for N in neighbors.keys():
                        #if N is a,b,c or d AND there is a cell there AND no junction => create junction